// CURRENT

watchForms

    watchUnsplashForm
        clearViewer
            removeResults
            hideParticlesBackGround
        showLoadingAnimation
        getUnsplashImages(query, limit)
            displayResultsUnsplash
                makeUnsplashHtmlResults(imageDetails)
                showResults
                removeLoadingAnimation
        scrollToTop

    watchBehanceForm
        clearViewer
            removeResults
            hideParticlesBackGround
        showLoadingAnimation
        getBehanceProjects(query, time)
            displayResultsBehance
                makeBehanceHtmlResults(imageDetails)
                showResults
                removeLoadingAnimation
        scrollToTop

    watchFontsForm
      clearViewer
          removeResults
          hideParticlesBackGround
      showLoadingAnimation
      getGoogleFonts(sortBy)
          displayResultsGoogleFonts
              makeHtmlFontLink(encodedFontFamily);
              makeFontsResultsHtml(fontFamily, fontCategory);
              showResults;
              removeLoadingAnimation;
      scrollToTop

    watchQuoteButton
      clearViewer
          removeResults
          hideParticlesBackGround
      showLoadingAnimation
      getRandomQuote
          displayResultsQuotes
              hideResults
              makeQuoteHtml(content, title, link)
              showResults
              activateParticlesBackground
              removeLoadingAnimation
      scrollToTop


// PROPOSED

watchForms

    watchUnsplashForm
        TransitionOut
            hideResults
            activateParticlesBackGround
            toggleLoadingAnimation
        getUnsplashImages(query, limit)
            displayResultsUnsplash
                makeUnsplashHtmlResults(imageDetails)
        TransitionIn
            showResults
            toggleLoadingAnimation
            scrollToTop

    watchBehanceForm
        HandleTransitionIn
            toggleResults
            toggleParticlesBackGround
            toggleLoadingAnimation
        getBehanceProjects(query, time)
            displayResultsBehance
                makeBehanceHtmlResults(imageDetails)
        HandleTransitionOut
            showResults
            toggleLoadingAnimation
            scrollToTop

    watchFontsForm
        HandleTransitionIn
            toggleResults
            toggleParticlesBackGround
            toggleLoadingAnimation
        getGoogleFonts(sortBy)
            displayResultsGoogleFonts
                makeHtmlFontLink(encodedFontFamily);
                makeFontsResultsHtml(fontFamily, fontCategory);
        HandleTransitionOut
            showResults;
            toggleLoadingAnimation
            scrollToTop

    watchQuoteButton
        HandleTransitionIn
            toggleResults
            toggleParticlesBackGround
            toggleLoadingAnimation
        getRandomQuote
            displayResultsQuotes
                hideResults
                makeQuoteHtml(content, title, link)
                toggleParticlesBackground 
        HandleTransitionOut
            showResults
            toggleLoadingAnimation
            scrollToTop
